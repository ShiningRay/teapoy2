
%h3
  %a{:href=>'#comment_form'} ↓
  =@comments.size
  条评论

.comments-list
  -if @comments.size > 0 and logged_in? and !params[:read]
    = cache [:collapse_mobile, current_user.id, @article], :expires_in => 1.minute do
      - if (read_to = current_user.has_read_article?(@article)) && read_to > 0
        = "已经折叠了#{read_to}个已读评论"
        - @comments = @comments[read_to..-1]
        %br
        %a{:href=> url_for('read'=>'full'), :class=>"show_readed manage_readed"}
          查看所有
      = render_comments @comments
  - else
    = render_comments @comments
- unless @article.closed?
  = comment_form
- else
  禁止评论
