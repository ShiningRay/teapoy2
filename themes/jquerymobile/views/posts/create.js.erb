$(window).one('pagechange', function(){
	var id = '#post_<%= @post.id.to_s %>';
	if($(id).size() == 0){
		$('.comments-list').append('<%= j render(@post) %>');
	}
	$.scrollTo(id);
	$('.ui-content').timeago('refresh');
});

$('[data-role=dialog]').dialog( "close" );