- if picture.picture.file?
  - if @list_view
    = link_to image_tag(picture.picture(:longsmall), :alt => picture.content.mb_chars[0,50], :style => picture.dim_style_size(:longsmall)),  |
              article_path(@group, picture.article), |
              :id => "picture-#{picture.id}",  |
              :title => (picture.article.title || picture.content.mb_chars[0,10])
  - else
    - if picture.picture.content_type =~ /gif/i
      = image_tag(picture.picture(:original), :alt => (picture.article.title || picture.content.mb_chars[0,10]))
    - else
      = link_to image_tag(picture.picture(:medium), :alt => picture.content.mb_chars[0,50]),  |
                picture.picture.url(:large), |
                :class => 'picture',  |
                :id => "picture-#{picture.id}",  |
                :title => (picture.article.title || picture.content.mb_chars[0,10])
      =# link_to picture.is_original? ? '给这张图片配上可爱的台词' : '我配的台词更好',draw_picture_path(picture) %>
      = render :partial => 'posts/post', :object => picture
