!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
-ie_html :class => 'no-js' do
  = render 'head'
  - today = Date.today
  %body{ :lang => 'en', :class => "#{controller.controller_name} #{controller.controller_name}-#{controller.action_name} two-col #{logged_in? && 'logged_in'} y#{today.year} m#{today.month} d#{today.day}" }
    = render 'header'
    #container.container.cf
      = content_for?(:main) ? (yield :main) : yield
    #gototop
      %a{:href=>"#top", :onclick=>"window.scrollTo(0,0);return false;"}
        回顶部
    %footer#footer
      = render 'footer'
    -# Javascript at the bottom for fast page loading
    = include_javascripts
    = show_login
