%h1 更新您的密码

%p 请输入新的密码并提交
- if @user && @user.errors
  - for m in @user.errors
    = m
= form_tag password_reset_path, :method => :put do
  %p
    %label
      密码
      %br
      = password_field_tag :password
  %p
    %label
      确认密码
      %br
      = password_field_tag :password_confirmation
  %p= submit_tag "更新密码"

