<% id = dom_id @post %>

$('#<%= id %> .midcol').addClass('likes').removeClass('dislikes unvoted');
$('#<%= id %> .up').text("<%= post.pos %>");
$('#<%= id %> .down').text('<%= @post.neg %>');

if (_hmt) {
    _hmt.push(['_trackEvent', 'posts', 'rate', <%= @post.id %>, 'up']);
};