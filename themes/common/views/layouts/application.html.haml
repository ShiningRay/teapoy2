-content_for :stylesheets do
  = stylesheet_link_tag current_theme_stylesheet_path('common')
- content_for :main do
  #main{ :role => 'main' }
    #content.cf= yield
    #sidebar.cf= render 'sidebar'
= render :template => 'layouts/onecolumn'
