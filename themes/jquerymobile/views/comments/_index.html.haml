
%h3
  %a{:href=>'#comment_form'} ↓
  =@comments.size
  条评论

%ul.comments-list
  = render_comments @comments
- if logged_in?
  - unless @article.closed?
    = link_to '发表评论', new_group_article_comment_path(@group, @article), data: {:role => 'button', :transition => 'slideup', :rel => 'dialog'} #comment_form
  - else
    禁止评论
- else
  = link_to '登录后发表评论', login_path, data: {role: 'button', transition: 'slidedown', rel: 'dialog'}