- if current_user.subscribed?(topic)
  = link_to '取消订阅', [:unsubscribe, topic.group, topic], method: :post, remote: true, id: dom_id(topic, 'unsubscribe')
- else
  = link_to '订阅', [:subscribe, topic.group, topic], method: :post, remote: true, id: dom_id(topic, 'subscribe')
