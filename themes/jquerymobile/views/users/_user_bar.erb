<% if logged_in? %>
  您好，<%= link_to_current_user :content_method => :login %>

  <%= link_to '发贴', posts_path %>
   |
  <%= link_to '小纸条', inbox_messages_path %><span style="color:#009900;font-size:9px;font-weight:bold;font-family:tahoma" id="unread_messages_count"></span>
  <%= link_to '提醒', notifications_path %><span style="color:#009900;font-size:9px;font-weight:bold;font-family:tahoma" id="unread_notifications_count"></span>
  |
  <%# link_to('超管平台', admin_dashboard_path) if current_user.has_role?('admin') or current_user.has_role?('doctor') %>
  <%= link_to '审核新帖', '/tickets/submit' %>
  |
  <%= link_to '邀请码', invitation_codes_path %>
  <%# link_to '领取奖励', :controller=>'my',:action=>'get_salary',:id=>current_user %>
  |
  <%# link_to '领取今日奖励', :controller=>'salaries',:action=>'index' %>

  <%= link_to("退出", logout_path, { :title => "Log out" }) %>
<% else %>
  您好，您可以 <%= link_to "登录", login_path, :style => 'border: none;', :class => 'need-login' %>
  或 <%= link_to("注册", signup_path) %>
<% end %>