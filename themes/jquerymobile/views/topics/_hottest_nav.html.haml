#mobile-nav
  = link_to t('nav.latest'), scoped_path(:order => 'latest')
  = link_to_unless @limit == 'day', t('topics.day'), scoped_path(:order => 'hottest', :limit => 'day')
  = link_to_unless @limit == 'week', t('topics.week'), scoped_path(:order => 'hottest', :limit => 'week')
  = link_to_unless @limit == 'month', t('topics.month'), scoped_path(:order => 'hottest', :limit => 'month')
  = link_to_unless @limit == 'year', t('topics.year'), scoped_path(:order => 'hottest', :limit => 'year')
