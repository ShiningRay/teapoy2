%h3 最近新建小组
%ul
  - Group.not_pending.order('created_at desc').limit(5).each do |group|
    %li
      = link_to topics_path(group) do
        = image_tag group.icon.thumb.url, :alt => group.name, :size => '48x48'
      %h4= link_to group.name, topics_path(group)
      %p= group.description

